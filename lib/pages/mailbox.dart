import 'package:flutter/material.dart';

class MailboxPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Your Mailbox is empty.'),
      ),
    );
  }
}
